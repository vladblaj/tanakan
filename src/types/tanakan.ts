export type SupaUser = {
  userId: string;
  name: string;
  presence_ref: string;
  avatar: string;
  online_at: string;
};

export type UserKeys = {
  username: string;
  publicKey: string;
  privateKey: string;
};
